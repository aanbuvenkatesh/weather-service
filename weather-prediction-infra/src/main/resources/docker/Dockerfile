FROM openjdk:8-jre

ENV ARTIFACT_ID weather-prediction.jar
ENV JAVA_OPTS "-Xmx1024m"
ENV WEATHER_APP_HOME /opt/weather

COPY ${ARTIFACT_ID} ${WEATHER_APP_HOME}/${ARTIFACT_ID}

WORKDIR ${WEATHER_APP_HOME}

ENTRYPOINT ["sh", "-c"]
CMD ["java -jar ${ARTIFACT_ID}"]